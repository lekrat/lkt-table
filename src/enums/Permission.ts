export enum Permission {
    Create = 'create', // Create inline items
    Update = 'update', // Save changes
    Edit = 'edit', // Displays edit button
    Drop = 'drop', // Displays drop button
    Sort = 'sort', // Sort
}